{
    "name": "css-framework",
    "version": "1.0.0",
    "description": "A class-free CSS framework",
    "main": "index.js",
    "scripts": {
        "lint": "stylelint 'src/**/*.css' && prettier --check '**/*.{js,css,json,md,html}'",
        "test": "echo \"Error: no test specified\" && exit 1",
        "build": "cat node_modules/modern-normalize/modern-normalize.css src/css/*.css src/css/themes/bulma.css > dist/css-framework.css"
    },
    "keywords": [
        "css",
        "framework",
        "classless",
        "class-free"
    ],
    "author": "Evgeny Orekhov",
    "license": "MIT",
    "devDependencies": {
        "@twinscom/stylelint-config": "^2.0.1",
        "husky": "^3.0.7",
        "lint-staged": "^9.4.0",
        "modern-normalize": "^0.5.0",
        "prettier": "^1.18.2",
        "stylelint": "^11.0.0"
    },
    "husky": {
        "hooks": {
            "pre-commit": "lint-staged",
            "pre-push": "npm run lint"
        }
    },
    "lint-staged": {
        "*.{js,css,json,md,html}": [
            "prettier --write",
            "git add"
        ],
        "*.css": [
            "stylelint --fix",
            "git add"
        ]
    }
}
